Git Hub Details


git init       // this is to initialise the git repo 

git remote add origin  "URL"              // this is used to add the origin url of repository

git remote -v        //to check connection status

git add "file name"   // which all files need to update to repository

git commit -m "comments"    // we can get a chance to add comments to the bundle changeset

git push origin master       // push the changed files bundle to the web repository

git status           //this will help us to know which all files are changed

